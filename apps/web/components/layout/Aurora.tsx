// DESIGN.md (Nike) forbids decorative gradients/background chrome — the canvas is
// pure white and all chromatic energy is reserved for photography/signal. So this
// renders nothing; kept as a no-op seam in case a future brand wants it back.
export function Aurora() {
  return null;
}
